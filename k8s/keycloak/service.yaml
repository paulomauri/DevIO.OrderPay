apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: orderpay
spec:
  selector:
    app: keycloak
  ports:
    - port: 8085
      targetPort: 8085
      name: http
    - port: 9000
      targetPort: 9000
      name: management
  type: LoadBalancer
