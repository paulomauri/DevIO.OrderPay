{
  "name": "orderpay-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright end-to-end tests for DevIO.OrderPay (browser → nginx → Next.js → WebApi → Keycloak → SQL Server)",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0",
    "wait-on": "^8.0.1"
  }
}
